This hasn't been packaged yet so for thsoe with IDE's are the only ones that can run the .py file as well as a couple of other things which I will mention here.
- Create a plain excel file(called Original) in a folder where you will run this code. Call that folder search_history
